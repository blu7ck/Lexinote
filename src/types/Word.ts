export interface Word {
    id: string
    group_id: string
    user_id: string
    title: string
    folder_letter: string
    formatted_notes: string
    image_url: string
    created_at: string
    created_by: string // join'li kullanımda
  }
  